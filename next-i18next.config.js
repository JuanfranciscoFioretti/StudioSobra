module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'es'], // Adjust to your supported locales
  },
  // Optional: Add other configuration options (e.g., ns, backend) if needed
};